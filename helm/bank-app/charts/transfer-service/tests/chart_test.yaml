suite: transfer-service
release:
  name: bank-app
templates:
  - deployment.yaml
  - service.yaml
tests:
  - it: renders transfer-service deployment
    template: deployment.yaml
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: bank-app-transfer-service
      - equal:
          path: spec.template.spec.containers[0].ports[0].containerPort
          value: 8086
