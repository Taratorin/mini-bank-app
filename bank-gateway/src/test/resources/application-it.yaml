spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: accounts-service
              uri: ${ROUTES_ACCOUNTS_URI}
              predicates:
                - Path=/accounts/**
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://127.0.0.1:1/realms/test